{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Tile settings window.",
  "windows": ["settings"],
  "permissions": ["core:default", "opener:default"]
}
