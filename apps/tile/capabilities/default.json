{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Tile settings, about, welcome and update windows.",
  "windows": ["settings", "about", "welcome", "updates"],
  "permissions": ["core:default", "opener:default"]
}
