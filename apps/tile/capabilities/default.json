{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Tile settings, about, and update windows.",
  "windows": ["settings", "about", "updates"],
  "permissions": ["core:default", "opener:default"]
}
