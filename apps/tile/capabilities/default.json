{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Tile settings, about and welcome windows.",
  "windows": ["settings", "about", "welcome"],
  "permissions": ["core:default", "core:window:allow-close", "opener:default"]
}
