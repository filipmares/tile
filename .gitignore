# Rust / Cargo
/target
**/target

# Node / frontend
node_modules
dist
apps/tile/ui/node_modules
apps/tile/ui/dist

# Tauri build output
apps/tile/target
gen/
**/gen/schemas

# Generated by release.yml when Windows signing secrets are present; it holds
# the Azure Artifact Signing account and profile names, and must never be
# committed — a checked-in signCommand breaks every unsigned local build.
apps/tile/tauri.signing.conf.json

# OS cruft
.DS_Store
Thumbs.db

# Editors / IDEs
.idea/
.vscode/*
!.vscode/tasks.json
*.swp
*~

# Logs
*.log

# Copilot workflow artifacts
.copilot-tracking/

# Impeccable design-hook scratch state
.impeccable/hook.cache.json
